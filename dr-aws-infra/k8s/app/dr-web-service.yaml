apiVersion: v1
kind: Service
metadata:
  name: dr-web-service
  labels:
    app: dr-web

spec:
  type: NodePort
  selector:
    app: dr-web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080