apiVersion: v1
kind: PersistentVolume
metadata:
  name: dr-efs-pv
spec:
  capacity:
    storage: 5Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
  storageClassName: dr-efs-sc
  csi:
    driver: efs.csi.aws.com
    volumeHandle: __EFS_ID__